import { type ReactElement } from "react";

import { Hero } from "components/HeroSection";

const Home = (): ReactElement => {
  return (
    <main className="">
      <Hero />
    </main>
  );
};

export default Home;
