import type { ReactNode } from 'react';

import { Hero } from 'components/HeroSection';

const Home = (): ReactNode => {
  return (
    <main>
      <Hero />
    </main>
  );
};

export default Home;
